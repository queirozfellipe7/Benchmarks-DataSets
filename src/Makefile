# Default
all: install_gawk set_permissions

# Instalar gawk
install_gawk:
    sudo apt-get update
    sudo apt-get install gawk

# Definir permissões
set_permissions:
    chmod +x script.sh
# Executar Script
 sudo ./script

